apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: db-url-conversion
  namespace: wiseling
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: db-url-conversion
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: wiseling/db-urls-sgp
        property: conversion