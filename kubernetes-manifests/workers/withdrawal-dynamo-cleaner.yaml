apiVersion: apps/v1
kind: Deployment
metadata:
  name: withdrawal-dynamo-cleaner
  namespace: wiseling
spec:
  replicas: 1
  selector:
    matchLabels:
      app: withdrawal-dynamo-cleaner
  template:
    metadata:
      labels:
        app: withdrawal-dynamo-cleaner
    spec:
      serviceAccountName: wiseling-sa
      containers:
      - name: withdrawal-dynamo-cleaner
        image: 359707702022.dkr.ecr.ap-southeast-2.amazonaws.com/wiseling/withdrawal-service:1.0.12
        command: ["python", "-m", "app.services.dynamo_cleaner"]
        envFrom:
          - configMapRef:
              name: wiseling-config
        env:
          - name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: db-url-withdrawal
                key: DATABASE_URL
        resources:
          limits:
            memory: "256Mi"
            cpu: "250m"
          requests:
            memory: "128Mi"
            cpu: "100m"